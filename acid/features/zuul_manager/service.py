# -*- coding: utf-8 -*-


def start_buildset(pipeline, buildset):
    # TODO (magickris93) Fill this function when start for buildset is ready.
    # Function should check if the buildset is already running and if it is
    # should apply proper behavior.
    pass


def stop_buildset(pipeline, buildset):
    # TODO (magickris93) Fill this function when stop for buildset is ready.
    # Function should check if the buildset is already running.
    pass
